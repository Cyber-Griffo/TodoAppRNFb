import { SplashStyles } from './SplashScreen.types'

export const getStyles = (): SplashStyles => {
  return {
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
  }
}
