import { SplashStyles } from './Splash.types'

export const getStyles = (): SplashStyles => {
  return {
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
  }
}
