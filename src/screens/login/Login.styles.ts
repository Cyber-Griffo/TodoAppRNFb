import { LoginStyles } from './Login.types'

export const getStyles = (): LoginStyles => {
  return {
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
  }
}
