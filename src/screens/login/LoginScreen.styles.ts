import { LoginStyles } from './LoginScreen.types'

export const getStyles = (): LoginStyles => {
  return {
    container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
    },
  }
}
